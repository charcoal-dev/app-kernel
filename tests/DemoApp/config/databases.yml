# Databases
# In this file, you can configure multiple databases.
databases:
  # By default, only one database is set up and its identifier key is labeled as "primary."
  primary:
    driver: sqlite
    host: 127.0.0.1
    port: 3306
    name: primary.sqlite
    username: root
    password: ~
